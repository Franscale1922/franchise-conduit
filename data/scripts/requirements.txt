# FDD Pipeline dependencies
# Install with: pip install -r data/scripts/requirements.txt

pdfplumber>=0.11.0
gspread>=6.0.0
google-auth>=2.28.0
openai>=1.0.0        # Optional — only needed when USE_AI=True in parse_fdd.py
